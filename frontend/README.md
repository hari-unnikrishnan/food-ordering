# Restaurant POS (React + Django)

## Backend
- Run Django:
  - `cd backend`
  - `python manage.py runserver`

API base (default): `http://localhost:8000`

## Frontend
- Install deps:
  - `cd frontend`
  - `npm install`
- Run dev server:
  - `npm run dev`

Open: `http://localhost:5173`

